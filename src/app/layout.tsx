import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Youssef Alkhodary | Portfolio",
  description: "Professional portfolio showcasing my work, skills, and experience in content creation and digital media.",
  keywords: ["Youssef Alkhodary", "Content Creator", "Digital Media", "Portfolio", "Social Media"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} bg-white dark:bg-gray-900 text-gray-900 dark:text-white`}>
        {children}
      </body>
    </html>
  );
}
